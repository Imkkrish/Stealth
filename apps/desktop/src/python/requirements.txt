# Stealth client v2 — light. No LLM SDKs (server handles those), no torch.

# Async + Socket.IO server (overlay <-> Python) and client (Python -> GCP)
aiohttp==3.9.5
python-socketio[asyncio_client]>=5.11.0
python-engineio>=4.9.0

# Local STT (faster-whisper / CTranslate2 — no PyTorch)
faster-whisper>=1.0.0

# Audio capture
sounddevice

# Resume parsing (parsed once locally, parsed text sent to server)
pypdf>=4.0.0
python-docx>=1.0.0

# Vision/Screen capture
mss==9.0.1
Pillow

# macOS Window Management (Window Watcher)
pyobjc-framework-Quartz
pyobjc-framework-Cocoa
pyobjc-framework-ApplicationServices

# Data processing
numpy

# Utilities
certifi
